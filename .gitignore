# Node.js dependencies and build artifacts
# These can be regenerated from package.json and should never be tracked
frontend/node_modules/
frontend/build/
frontend/dist/
frontend/.env.local
frontend/.env.development.local
frontend/.env.test.local
frontend/.env.production.local

# Python virtual environment and cache files
# These can be recreated from requirements and contain system-specific paths
backend/venv/
backend/app/__pycache__/
backend/app/services/__pycache__/
backend/*.pyc
backend/*.pyo
backend/.pytest_cache/

# IDE and editor configuration files
# These are personal preferences and often contain system-specific paths
.vscode/
.idea/
*.swp
*.swo
*~

# Operating system generated files
# These are created automatically and serve no purpose in version control
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs and temporary files
# These change constantly and don't represent intentional code changes
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables and secrets
# These often contain sensitive information that shouldn't be shared
.env
.env.local
.env.development
.env.test
.env.production

# Coverage reports and test artifacts
# These are generated during testing and can be recreated
coverage/
*.lcov
